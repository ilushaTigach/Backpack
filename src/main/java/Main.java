public class Main {

    public static void main(String[] args) {
//        Scanner scanner = new Scanner(System.in);
//Class Items.Item
//          Item item = new Item().setName(scanner.nextLine()).setSize(scanner.nextInt());
//          System.out.println("Название вещи " + item.getName());
//          System.out.println("Размер вещи " + item.getSize());


//Class menu
             Menu menu = new Menu();
             menu.startMenu();

//Class Jacksontest
//        JacksonTest jacksonTest = new JacksonTest();
//        jacksonTest.jsonToPojoString();

//Class Backpack
//       Backpack backpack = new Backpack().addItemInBackpack(item).takeItemFromBackpack();

//Class Items.Weapon

        /*Items.Weapon weapon = new Items.Weapon();
        boolean test = true;
        do{
            try {
                System.out.println("Введите качество оружия от 1 до 10: ");
                weapon.setQuality(scanner.nextInt());
                test = false;
            } catch (exception.QualityException e) {
                System.err.println((e.getMessage()));

            }
        }while (test);
        System.out.println(weapon.getQualityDescription());*/

        //DataRecorder dataRecorder = new DataRecorder().writeItem(item);


    }
}